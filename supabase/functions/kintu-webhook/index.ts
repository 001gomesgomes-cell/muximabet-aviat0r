// Muxima Bet — webhook de confirmação de depósito (Kintu)
// Identifica o lead e credita o saldo (valor + bónus). Idempotente por external_id.
// Regista cada payload em public.webhook_logs para diagnóstico.
// (Fonte de referência; a versão em execução foi publicada via deploy_edge_function.)
